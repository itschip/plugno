import { SafeAreaView, View } from "react-native";

export const RequestSummaryScreen = () => {
  return (
    <SafeAreaView className="flex-1 bg-white">
      <View></View>
    </SafeAreaView>
  );
};
